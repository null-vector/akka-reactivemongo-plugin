package org.nullvector

object Fields {
  val persistenceId = "pid"
  val sequence = "sn"
  val from_sn = "from"
  val to_sn = "to"
  val events = "events"
  val event_ts = "ts"
  val payload = "p"
  val manifest = "manifest"
  val tags = "_tg"
  val snapshot_ts = "ts"
  val snapshot_payload = "s2"
}
